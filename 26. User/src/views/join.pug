extends templates/layout

block content 
  if errorMessage 
    span=errorMessage
  form(method="POST")
    input(name="username", type="text", placeholder="사용자명", required=true)
    input(name="password", type="password", placeholder="비밀번호", required=true)
    input(name="password2", type="password", placeholder="확인 비밀번호", required=true)
    input(name="name", type="text", placeholder="성명", required=true)
    input(type="submit", value="가입")
    hr
    div 
      span 계정이 혹시 있으신가요? 
      a(href="/login") Log in now &rarr;
